import sys
import re




alignment = open(sys.argv[1], "rU")

for basepos in len(alignment[0,-1]):
	print basepos

#for basepos in len(alignment[:]):
#	all_gaps = True
	
#		if ( sequence[j][i] != '-'):
			#all_gaps = False
			#last
			
#		for seq_nr in range(0,len(sequence)):
			#sequence[j][i] = 'P'
		
			
#for sequence in alignment:
	#sequence = re.replace('r/P/','//', sequence)
	#print sequence
	
